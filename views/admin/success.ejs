<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Shipment Created Successfully</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/dashboard.css">
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f7fa;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }

    .card {
      background: white;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      text-align: center;
      max-width: 500px;
      width: 90%;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      color: #28a745;
      margin-bottom: 10px;
    }

    p {
      color: #555;
      font-size: 1.1em;
      margin-bottom: 15px;
    }

    .tracking-number {
      background: #f0f2ff;
      border: 2px dashed #667eea;
      border-radius: 10px;
      padding: 15px;
      margin: 20px 0;
      font-size: 1.4em;
      color: #4a4eea;
      font-weight: bold;
      letter-spacing: 1px;
    }

    .btn {
      display: inline-block;
      padding: 12px 25px;
      border-radius: 8px;
      background: #667eea;
      color: white;
      text-decoration: none;
      font-weight: 600;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .btn:hover {
      background: #5568d3;
      transform: translateY(-2px);
    }

    .btn-secondary {
      background: #6c757d;
      margin-left: 10px;
    }

    .btn-secondary:hover {
      background: #5a6268;
    }

    .icon {
      font-size: 3em;
      color: #28a745;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="icon"><i class="fas fa-check-circle"></i></div>
    <h1>Shipment Created Successfully!</h1>
    <p>Your shipment has been added to the system.</p>

    <p><strong>Tracking Number:</strong></p>
    <div class="tracking-number"><%= shipment.trackingNumber %></div>

    <!-- <a href="/track/<%= shipment.trackingNumber %>" class="btn">
      <i class="fas fa-search"></i> Track Shipment
    </a> -->
    <a href="/shipment/new" class="btn btn-secondary">
      <i class="fas fa-plus-circle"></i> Create Another
    </a>
  </div>
</body>
</html>
