<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shipment Tracking — <%= shipment.trackingNumber %></title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      /* exactly your style block — identical */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: white;
        min-height: 100vh;
        padding: 20px;
        color: #333;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
        animation: fadeIn 0.6s ease-in;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .header {
        background: white;
        padding: 30px;
        margin-bottom: 30px;
        text-align: center;
      }
      .header h1 {
        color: #667eea;
        margin-bottom: 10px;
        font-size: 2em;
      }
      .barcode-section {
        background: white;
        padding: 30px;
        margin-bottom: 30px;
        text-align: center;
      }
      .barcode {
        background: linear-gradient(90deg, #000 2px, transparent 2px) repeat-x;
        background-size: 8px 100%;
        height: 80px;
        margin: 20px auto;
        max-width: 400px;
        position: relative;
        border-radius: 5px;
      }
      .tracking-number {
        font-size: 1.5em;
        font-weight: bold;
        color: #667eea;
        margin-top: 15px;
        letter-spacing: 2px;
      }
      .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
      }
      .info-card {
        background: white;
        padding: 25px;
        transition: transform 0.3s ease;
        border-right: 1px solid #e0e0e0;
      }
      .info-card:last-child {
        border-right: none;
      }
      .info-card:hover {
        transform: translateY(-5px);
      }
      .info-card h2 {
        color: #667eea;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 1.3em;
      }
      .info-card h2 i {
        font-size: 1.2em;
      }
      .info-row {
        display: flex;
        justify-content: space-between;
        padding: 12px 0;
        border-bottom: 1px solid #f0f0f0;
      }
      .info-row:last-child {
        border-bottom: none;
      }
      .info-label {
        font-weight: 600;
        color: #666;
      }
      .info-value {
        color: #333;
        text-align: right;
      }
      .status-badge {
        display: inline-block;
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: bold;
        font-size: 0.9em;
      }
      .status-in-transit {
        background: #4caf50;
        color: white;
      }
      .status-on-hold {
        background: #ff9800;
        color: white;
      }
      .status-delivered {
        background: #2196f3;
        color: white;
      }
      .timeline-section {
        background: white;
        padding: 30px;
      }
      .timeline-section h2 {
        color: #667eea;
        margin-bottom: 30px;
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 1.5em;
      }
      .timeline {
        position: relative;
        padding-left: 50px;
      }
      .timeline::before {
        content: "";
        position: absolute;
        left: 20px;
        top: 0;
        bottom: 0;
        width: 3px;
        background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
      }
      .timeline-item {
        position: relative;
        margin-bottom: 40px;
        animation: slideIn 0.5s ease-out;
      }
      @keyframes slideIn {
        from {
          opacity: 0;
          transform: translateX(-20px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }
      .timeline-item::before {
        content: "";
        position: absolute;
        left: -37px;
        top: 0;
        width: 15px;
        height: 15px;
        border-radius: 50%;
        background: white;
        border: 4px solid #667eea;
        box-shadow: 0 0 0 4px white;
      }
      .timeline-item.active::before {
        background: #667eea;
        animation: pulse 2s infinite;
      }
      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.2);
        }
      }
      .timeline-content {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 10px;
        border-left: 4px solid #667eea;
      }
      .timeline-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
        flex-wrap: wrap;
        gap: 10px;
      }
      .timeline-date {
        font-weight: bold;
        color: #667eea;
      }
      .timeline-location {
        font-size: 1.1em;
        font-weight: 600;
        color: #333;
        margin-bottom: 5px;
      }
      .timeline-status {
        font-size: 0.9em;
        color: #666;
      }
      .timeline-remarks {
        background: #fff3cd;
        padding: 15px;
        border-radius: 8px;
        margin-top: 10px;
        border-left: 4px solid #ffc107;
        font-size: 0.95em;
        color: #856404;
      }
      .timeline-remarks i {
        margin-right: 8px;
        color: #ffc107;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      th {
        background: #667eea;
        color: white;
        padding: 15px;
        text-align: left;
        font-weight: 600;
      }
      td {
        padding: 15px;
        border-bottom: 1px solid #f0f0f0;
      }
      tr:hover {
        background: #f8f9fa;
      }
      @media (max-width: 768px) {
        body {
          padding: 10px;
        }
        .header h1 {
          font-size: 1.5em;
        }
        .info-grid {
          grid-template-columns: 1fr;
        }
        .info-card {
          border-right: none;
          border-bottom: 1px solid #e0e0e0;
        }
        .info-card:last-child {
          border-bottom: none;
        }
        .timeline {
          padding-left: 40px;
        }
        .timeline::before {
          left: 15px;
        }
        .timeline-item::before {
          left: -32px;
        }
        .tracking-number {
          font-size: 1.2em;
        }
        .barcode {
          height: 60px;
        }
        table {
          font-size: 0.9em;
        }
        th,
        td {
          padding: 10px 8px;
        }
        .timeline-header {
          flex-direction: column;
          align-items: flex-start;
        }
      }
      @media (max-width: 480px) {
        .info-row {
          flex-direction: column;
          gap: 5px;
        }
        .info-value {
          text-align: left;
        }
        table {
          display: block;
          overflow-x: auto;
          white-space: nowrap;
        }
      }
    </style>
  </head>
  <body>
    <%- include('../partials/header') %>
    <div class="container">
      <div class="header">
        <h1><i class="fas fa-shipping-fast"></i> Shipment Tracking</h1>
        <p>Track your package in real-time</p>
      </div>

      <div class="barcode-section">
        <h2>Tracking Barcode</h2>
        <div class="barcode"></div>
        <div class="tracking-number"><%= shipment.trackingNumber %></div>
      </div>

      <div class="info-grid">
        <div class="info-card">
          <h2><i class="fas fa-user-tie"></i> Shipper Information</h2>
          <div class="info-row">
            <span class="info-label">Name:</span>
            <span class="info-value"><%= shipment.shipperName || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Address:</span>
            <span class="info-value"
              ><%= shipment.shipperAddress || '—' %></span
            >
          </div>
          <div class="info-row">
            <span class="info-label">Contact:</span>
            <span class="info-value"
              ><%= shipment.shipperContact || '—' %></span
            >
          </div>
          <div class="info-row">
            <span class="info-label">Email:</span>
            <span class="info-value"><%= shipment.shipperEmail || '—' %></span>
          </div>
        </div>

        <div class="info-card">
          <h2><i class="fas fa-user"></i> Receiver Information</h2>
          <div class="info-row">
            <span class="info-label">Name:</span>
            <span class="info-value"><%= shipment.receiverName || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Address:</span>
            <span class="info-value"
              ><%= shipment.receiverAddress || '—' %></span
            >
          </div>
          <div class="info-row">
            <span class="info-label">Contact:</span>
            <span class="info-value"
              ><%= shipment.receiverContact || '—' %></span
            >
          </div>
          <div class="info-row">
            <span class="info-label">Email:</span>
            <span class="info-value"><%= shipment.receiverEmail || '—' %></span>
          </div>
        </div>

        <div class="info-card">
          <h2><i class="fas fa-plane"></i> Shipment Status</h2>
          <div class="info-row">
            <span class="info-label">Service Type:</span>
            <span class="info-value"><%= shipment.serviceType || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Departure Date:</span>
            <span class="info-value"><%= shipment.departureDate || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Expected Delivery:</span>
            <span class="info-value"
              ><%= shipment.expectedDelivery || '—' %></span
            >
          </div>
          <div class="info-row">
            <span class="info-label">Destination:</span>
            <span class="info-value"><%= shipment.destination || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Description:</span>
            <span class="info-value"><%= shipment.description || '—' %></span>
          </div>
          <div class="info-row">
            <span class="info-label">Total Weight:</span>
            <span class="info-value"
              ><%= shipment.totalWeight || '—' %> kg</span
            >
          </div>
        </div>
      </div>

      <div class="info-card" style="margin-bottom: 30px">
        <h2><i class="fas fa-box"></i> Package Details</h2>
        <table>
          <thead>
            <tr>
              <th>Qty</th>
              <th>Weight (kg)</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <% if (shipment.packages && shipment.packages.length) { %> <%
            shipment.packages.forEach(p => { %>
            <tr>
              <td><%= p.qty %></td>
              <td><%= p.weight %></td>
              <td><%= p.description %></td>
            </tr>
            <% }) %> <% } else { %>
            <tr>
              <td colspan="3" style="text-align: center; color: #777">
                No package details
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>

      <div class="timeline-section">
        <h2><i class="fas fa-route"></i> Travel History</h2>
        <div class="timeline">
          <% if (shipment.statusHistory && shipment.statusHistory.length) { %>
          <% shipment.statusHistory.slice().reverse().forEach((st, idx) => { %>
          <div class="timeline-item <%= idx === 0 ? 'active' : '' %>">
            <div class="timeline-content">
              <div class="timeline-header">
                <div class="timeline-date">
                  <i class="far fa-calendar"></i> <%= st.date %> • <%= st.time
                  %>
                </div>
                <% let badgeClass = ''; const s = (st.theStatus ||
                '').toLowerCase(); if (s.includes('in transit')) badgeClass =
                'status-in-transit'; else if (s.includes('on hold')) badgeClass
                = 'status-on-hold'; else if (s.includes('delivered')) badgeClass
                = 'status-delivered'; %>
                <span class="status-badge <%= badgeClass %>"
                  ><%= st.theStatus %></span
                >
              </div>
              <div class="timeline-location">
                <i class="fas fa-map-marker-alt"></i> <%= st.airportName %>
              </div>
              <div class="timeline-status"><%= st.description %></div>
              <% if (st.remark) { %>
              <div class="timeline-remarks">
                <i class="fas fa-exclamation-triangle"></i>
                <strong>Action Required:</strong> <%= st.remark %>
              </div>
              <% } %>
            </div>
          </div>
          <% }) %> <% } else { %>
          <div class="timeline-item">
            <div class="timeline-content">
              <div class="timeline-header">
                <div class="timeline-date">
                  <i class="far fa-calendar"></i> —
                </div>
                <span class="status-badge">No updates</span>
              </div>
              <div class="timeline-location">—</div>
              <div class="timeline-status">
                There are currently no updates for this shipment.
              </div>
            </div>
          </div>
          <% } %>
        </div>
      </div>
    </div>
    <%- include('../partials/footer') %>

    <script>
      // Add smooth scroll animation & barcode builder (kept your script)
      document.addEventListener("DOMContentLoaded", function () {
        const timelineItems = document.querySelectorAll(".timeline-item");

        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.style.opacity = "1";
                entry.target.style.transform = "translateX(0)";
              }
            });
          },
          { threshold: 0.1 }
        );

        timelineItems.forEach((item) => {
          item.style.opacity = "0";
          item.style.transform = "translateX(-20px)";
          item.style.transition = "all 0.5s ease-out";
          observer.observe(item);
        });

        // Animate barcode
        const barcode = document.querySelector(".barcode");
        let bars = "";
        for (let i = 0; i < 50; i++) {
          const height = Math.random() * 40 + 40;
          const width = Math.random() * 2 + 2;
          bars += `<div style="display:inline-block;width:${width}px;height:${height}%;background:#000;margin:0 1px;vertical-align:bottom;"></div>`;
        }
        barcode.innerHTML = bars;
      });
    </script>
  </body>
</html>
